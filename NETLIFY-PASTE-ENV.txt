Netlify → your site → Site configuration → Environment variables

Add these 4 (copy/paste):

1) DATABASE_URL
   Get free at https://neon.tech → New project → Connection string
   Paste the full postgres://... string here

2) AUTH_SECRET
   Any long random text (example: CarrieSite2026SecretKeyRandomChars)

3) ADMIN_EMAIL
   author@example.com

4) ADMIN_PASSWORD
   change-me-on-first-login

Then: Deploys → Trigger deploy → Deploy site

Sign in at yoursite.netlify.app/login with ADMIN_EMAIL and ADMIN_PASSWORD
